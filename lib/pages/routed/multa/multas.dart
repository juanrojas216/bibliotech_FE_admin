import 'package:flutter/material.dart';

class MultasPage extends StatelessWidget {
  const MultasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Multas"));
  }
}
